---
description: 
globs: 
alwaysApply: false
---
Use this rule when working on layout, UI styling, or theming tasks.
Regels:
	•	Use the DiscGolfMetrix visual style as reference.
	•	(Optional) Use EU-inspired blue-yellow color palette if branding applies.
Tags: @/layouts, @/css, @/components/ui

PR‑checklist (toevoegen aan .cursor/rules/manual.mdc):
	•	Heb ik geen nieuwe SW‑bestanden/registraties toegevoegd? (We gebruiken GenerateSW.)
	•	Gaan al mijn PB‑calls via const { pb } = usePocketbase()?
	•	Komt mijn API/baseURL uit env, niet hardcoded?
	•	Gebruik ik getFileUrl(*) i.p.v. handmatig URL’s opbouwen?
	•	npm lint en npm build zijn groen.